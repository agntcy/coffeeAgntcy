# Tasks

## 1. Confirm the repo-tooling spec matches current behavior

- [x] 1.1 For each requirement in `specs/repo-tooling/spec.md`, run its corresponding command (`task setup`, `task dashes:check`/`fix`, `task shell:lint`/`fmt`, `task workflows:lint`, `task workflows:check-permissions`, `task pins:check`, `task check:all`) against this repo as it stands today and confirm the observed behavior matches that requirement's scenarios exactly; this capability documents already-shipped behavior, so no code should need to change here - note and resolve anything that doesn't match (a genuine drift between the rule prose and actual behavior) rather than silently editing the spec to match a bug

## 2. Extract ci-gate's Wait/Summarize into scripts, with the sibling-exclusion fix

- [x] 2.1 Write `scripts/ci-gate/wait-for-sibling-runs.sh`, porting the current inline "Wait" step's polling logic (settle delay, poll interval, consecutive-clean-polls, overall timeout, `runs.json` + `settled` output) to read its GitHub/timing context from environment variables with sensible defaults (`: "${VAR:=default}"`), and add `own_workflow_id` resolution (the `workflow_id` of the run matching `$GITHUB_RUN_ID` in the same API response) so the exclusion filter is `select(.id != $self) | select($ownwf == null or .workflow_id != $ownwf)` instead of `id`-only; verify by running it locally with a real `gh`-authenticated session against this repo's own recent commits and confirming it reports the same pending/settled state `ci-gate.yaml`'s current inline step would
- [x] 2.2 Write `scripts/ci-gate/summarize-ci-gate.sh`, porting the current inline "Summarize" step's table-building and pass/fail decision to read `SETTLED`/`GITHUB_RUN_ID`/a `runs.json` path from the environment (no `VALIDATE_OUTCOME` - CI Gate no longer validates anything itself, so there is no separate validation row in the table, only sibling-run rows), writing to `$GITHUB_STEP_SUMMARY` when set (falling back to stdout, without double-printing when there's no such file), and applying the same `own_workflow_id` exclusion as 2.1 when building the sibling-run rows; verify with a handful of hand-constructed `runs.json` fixtures covering all-pass, one-failing-sibling, and a still-`in_progress` second CI Gate run sharing `workflow_id`
- [x] 2.3 Add `ci-gate:wait` and `ci-gate:summarize` Taskfile tasks wrapping the two scripts above (matching this repo's `task <name>` convention, `desc`/`summary` included); verify `task --list` shows both and each runs its script unmodified
- [x] 2.4 Rewrite `.github/workflows/ci-gate.yaml` to a pure collector: remove the "Validate all workflow files" step and the "Bootstrap repo-local tooling" step entirely; "Wait for sibling workflow runs" and "Summarize and decide" call `scripts/ci-gate/wait-for-sibling-runs.sh`/`summarize-ci-gate.sh` directly (not via `task`, no toolchain bootstrap - they need only `bash`/`jq`/`gh`, already on the runner), passing through the env vars those scripts read (`GH_TOKEN`, `HEAD_SHA`, `SETTLED`); verify `actionlint`/`task workflows:lint` passes on the updated file
- [x] 2.5 Verify the deadlock fix directly: construct a `runs.json` fixture with two entries sharing one `workflow_id` (one matching `$GITHUB_RUN_ID`, the other `in_progress`) and confirm `wait-for-sibling-runs.sh` reports settled instead of waiting on the second entry, and that `summarize-ci-gate.sh` doesn't count that second entry as a failing sibling either
- [x] 2.6 Broaden `checks.yaml`'s triggers to match `ci-gate.yaml`'s exactly (`pull_request: {}` with no branch filter, `push` to `main` and tags, `workflow_dispatch`) so `checks.yaml` is unconditionally one of CI Gate's sibling runs on every commit CI Gate itself runs on; verify the trigger blocks are identical between the two files and `actionlint` passes

## 3. End-to-end verification

- [x] 3.1 Run `task check:all` and `task shell:lint` including the two new `scripts/ci-gate/*.sh` files and confirm both pass clean
- [x] 3.2 Confirm a live CI Gate run still behaves correctly end-to-end (wait/summarize, correct pass/fail, no validation step of its own) - needs an actual push or PR to exercise; user will trigger and verify this manually
