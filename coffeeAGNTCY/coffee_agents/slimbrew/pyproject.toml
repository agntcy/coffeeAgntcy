[project]
name = "slimbrew"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "pytest>=8.4.1",
    "slim-bindings==0.4.0",
]
