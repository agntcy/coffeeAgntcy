[project]
name = "corto"
version = "0.1.0"
description = ""
authors = [{ name = "Shridhar Shah", email = "shridhsh@cisco.com" }]
requires-python = ">=3.13,<3.14"
dependencies = [
    "a2a-sdk==0.3.20",
    "click==8.3.1",
    "coloredlogs==15.0.1",
    "dotenv==0.9.9",
    "fastapi==0.124.4",
    "httpx==0.28.1",
    "langchain-anthropic==1.7.1",
    "langchain-google-genai==4.2.0",
    "langchain-openai==0.3.34",
    "langchain-litellm==0.3.5",
    "langgraph==1.2.11",
    "langgraph-supervisor==0.0.31",
    "litellm==1.84.0",
    "pydantic==2.12.5",
    "python-dotenv==1.2.3",
    "requests==2.34.2",
    "starlette==0.50.0",
    "uvicorn==0.33.0",
    "ioa-observe-sdk==1.0.41",
    "agntcy-app-sdk==0.5.5",
    "slim-bindings==1.4.0",
    "slima2a==0.3.0",
    "azure-core==1.38.0",
    # Vulnerability fix for requests
    "langchain-core==1.6.2",
    "urllib3==2.7.0",
    "aiohttp==3.14.3",
    "pyasn1==0.6.4",
    "pypdf==6.18.0",
    "filelock==3.20.3",
    "marshmallow==3.26.2",
    "PyNaCl==1.6.2",
    "mcp==1.30.0",
    "cryptography==50.0.1",
    "grpcio==1.78.0",
    "grpcio-tools==1.78.0",
    "protobuf==6.33.5",
]

[project.optional-dependencies]
dev = [
    "pytest==7.4.4",
    "pytest-asyncio==0.23.8",
    "pytest-cov==4.1.0",
    "typing-extensions==4.15.0",
    "openai==2.54.0",
    "coloredlogs==15.0.1",
    "langchain-openai==0.3.34",
    "sentence-transformers==5.2.2",
    "ruff==0.14.14",
]

[tool.uv]
exclude-newer = "14 days"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["farm", "exchange"]

[tool.pytest.ini_options]
addopts = "--asyncio-mode=auto"
filterwarnings = ["error"]
markers = [
    "agents(names): marks tests that run for specific farms (e.g. @pytest.mark.farms([\"brazil\"]))",
]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
indent-width = 4
target-version = "py313"

[tool.ruff.format]
indent-style = "space"

[tool.ruff.lint]
# Indentation only (issue #747). Broader Ruff rules can land in a later PR.
# W191/E101 ban tabs and mixed indent; E111/E114/E117 enforce the 4-space width.
# E111/E114/E117 are preview-only, so preview is scoped to the linter here and
# does not affect `ruff format` output.
preview = true
select = ["W191", "E101", "E111", "E114", "E117"]
